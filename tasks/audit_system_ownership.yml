---
- name: Audit critical system ownership without sudo
  ansible.builtin.script: "{{ playbook_dir }}/files/audit_system_ownership.py"
  args:
    executable: /usr/bin/python3
  become: false
  changed_when: false
  check_mode: false
  tags: always
