# Scratch files
A
p
t.sh
notes
launch_instance.yml_

# Test evidence (screenshots, logs)
test-evidence/

# Personal dotfiles rsync'd to remote hosts — contains .bash_history, .claude.json, .npmrc etc
weekday/

# Embedded git repo — not a submodule
ansible-roles/

# Pre-built artifacts for Google Drive upload (outputs of build_gdrive_artifacts.sh)
gdrive-artifacts/

# Not part of the playbook — requires desktop Firefox profile
test_weblogic_jwt.py

# Local secrets — copy separately via scp, never commit
secrets.yml

# Ansible retry files
*.retry

# Python cache
__pycache__/
*.pyc

# Scratch/temp working files
tmp/

# Agent/session artefacts (added 2026-09-17)
.claude/
transcript.en.vtt

# Superseded by the browser_host role on the browser-host branch
setup_browser_host_enhanced.yml
setup_browser_host.yml.backup
